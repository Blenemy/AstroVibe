---
interface Props {
  className?: string;
  tag?: "h1" | "h2";
}

const { className, tag }: Props = Astro.props;
const Tag = tag || "h1";
---

<Tag
  class={`text-22 leading-28 font-medium md:text-[45px] md:leading-[64px] ${className ? className : ""}`}
>
  <slot />
</Tag>
